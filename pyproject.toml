[project]
name = "onnxforge"
version = "0.1.0"
requires-python = ">=3.11"
description = "Graph-level ONNX optimization for torch.onnx.export(dynamo=True) models, targeting ARM"
license = { text = "MIT" }
readme = "README.md"

dependencies = [
    "onnx>=1.14",
    "onnxruntime>=1.16",
    "onnxscript>=0.1",
    "onnxslim>=0.1",
    "numpy>=1.24",
    "pydantic-settings>=2.14.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0",
    "ruff>=0.4",
    "pyrefly>=1.0.0",
    # model zoo for building test / demo ONNX files
    "stackformers>=3.9",
    "timm>=1.0",
    "transformers>=4.40",
    "torch>=2.12",
]

[project.scripts]
onnxforge = "onnxforge.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/onnxforge"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pyrefly]
python-version = "3.11"
# Point at the uv venv so pyrefly finds torch, onnx, and other installed packages
python-interpreter-path = ".venv/bin/python"
project-includes = ["src"]
# Do not infer types from first-use (stricter)
infer-with-first-use = false

[tool.pytest.ini_options]
testpaths = ["tests"]
